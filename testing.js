//  ms_passed = 0
//     let randomIntervalTime = parseInt(Math.random() * (100 - 0) + 0)
//       for (let k = 0 ; k < 10000 ; k++) {
//         setInterval(() => {
//           const randomInterval = parseInt(Math.random() * (1000 - 0) + 100) // min max - 20 2000
//           if((k%2) - 1 === 0){
//             strobeLighting(counter)
//             // strobeLighting2()

//             ms_passed = randomInterval*counter
//             setTimeout(() => {strobeLighting(counter)}, ms_passed)
           
//         }, randomIntervalTime)
//       } 




        // counter = 0
        // ms_passed = 0
        // let randomIntervalTime = parseInt(Math.random() * (100 - 0) + 0)
        //   for (let k = 0 ; k < 10000 ; k++) {
        //     setInterval(() => {
        //       const randomInterval = parseInt(math.Random() * (1000 - 0) + 100) // min max - 20 2000
        //       if((k%2) - 1 === 0){
        //         strobeLighting(counter)
        //         ms_passed = randomInterval*counter
        //         setTimeout(() => {strobeLighting(counter)}, ms_passed)
        //         counter += 1
              
        //       }
        //     }, randomIntervalTime)
        //   }